{
  "buildCommand": "corepack pnpm install && corepack pnpm run build",
  "outputDirectory": "artifacts/sales-os/dist/public",
  "framework": "vite",
  "installCommand": "corepack pnpm install --frozen-lockfile"
}
